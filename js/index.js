const d3 = require('d3');
const startData = require('../data/start_data.json');

document.addEventListener('DOMContentLoaded', function () {
    d3.json("startData", function(error, data) {
        debugger
    });
});